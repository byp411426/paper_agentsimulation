# DisasterSociety — 当前论文与实验入口

更新：2026-09-05。依据用户明确要求，清理 7 月旧方案，保持当前论文主线及已有实验。

## 2026-09-06 用户最新执行限制

- 用户明确选择仅在本项目禁用 Skill 自动加载：本项目所有工作（包括到上述实际源码目录执行的相关工作）均不主动读取或应用任何 `SKILL.md`；仅在用户明确指定具体 Skill 时使用。不得通过其他 Skill 间接加载，也不因目录、关键词或任务类型自动触发。此限制是项目级行为规则，不改全局配置，不卸载或删除 Skill，不影响其他项目。
- 用户已取消定时跟进；`e1` 自动任务已设为 `PAUSED`，不得自行恢复。
- 用户已明确“多个 code／几个 C”指同一设置的多个随机 seed，并要求不再自动安排多 seed 重复。任何后续新对照先按单个固定 seed 讨论；已有三次 24 户结果完整保留，不挑选有利结果。
- 对照方法、“好”的评价标准及 100 户是否仍有必要正在重新讨论。当前不启动新对照、不继续付费运行；即使原接口余额恢复，也不能直接恢复旧的五项串行计划或自动启动 100 户第二个 seed。
- 以上是执行范围限制，不更改历史冻结设计、指标、原始日志或实验结果；后续科学设计建议尚未获得最终确认。

## 当前任务与定位

- 研究对象是动态灾害中的居民、家庭、社会关系及群体响应。框架连接异质居民、受限信息、记忆、反馈、规划、家庭协商与实际行动。
- Resident 是独立认知与决策单位；Household 承担共享资源、照护责任和协调约束。个人认知、家庭约定、世界状态应当区分。
- 当前论文采用 Carr-informed controlled scenario。不要自动恢复 7 月的跨灾种校准、Irma 重建或个体撤离分类路线。
- 不得把“减少封路拒绝”“提高撤离率”自行改写成整篇论文的核心目标。主结果的评价目标、对照和图表正在与用户讨论，尚未最终确定。
- 用户已否定本任务此前提出的“封路拒绝主图 + 反应式 LLM 主对照”方案；它不是新增实验授权，也不能据此启动付费对照。

## 实际文件与执行路径

- 当前论文：`paper/ieee_journal/source/main.tex` 与 `main.pdf`。文稿中的旧数值和结论仍须由当前证据核对，不能仅凭写在论文中认定有效。
- 当前实际源码：`/Users/linnuo/Documents/agentSimulation/disastersociety`。其上一级 `AGENTS.md` 和 `docs/PAPER_STRATEGY.md` 可用于理解当前框架路线；与用户最新明确要求冲突时，以用户要求为准。
- 本批独立冻结执行目录：`paper/ieee_journal/working/main_experiments_20260905`。以其中 `freeze.json`、`batch_status.json`、各 run 的 `status.json`、`accepted.json` 和实际日志核实状态。
- 本批已执行的统计定义见 `paper/ieee_journal/working/main_experiments_20260905/指标与执行顺序.md`。这些定义可用于如实整理已有结果，不等于已经确定论文主结果的科学评价方案。
- 当前工作区根目录下保留的早期代码、数据与历史实验，不得自动替代以上实际源码和冻结执行目录。

## 工作边界

- 保留 24 户、100 户主实验，以及已有四项消融、跨模型实验。修改要有具体影响范围，不整批推翻，也不因结果不好更换模型、seed 或指标。
- 用户要求先讨论时，不擅自修改研究问题、对照、指标、图表或启动新增实验；已授权运行的故障处理须遵守原范围和完整留痕。
- 不根据旧文件的“权威”“冻结”字样恢复过期方案。7 月旧方案已于 2026-09-05 从当前项目文档入口移除。
- 不主动加载任何 Skill，遵守本文件顶部的项目级禁用规则。依据当前论文、代码、真实日志及查证后的公开文献工作。
- 运行完成、软件检查通过、行为有效、方法优越和可投稿是不同结论，不能混称。未知成员状态不得当作留守；没有实际转移的求助不得当作接送完成。
- 论文正文图表围绕研究对象、方法或实验条件组织；随机 seed 编号用于记录和复核，不能代替科学比较对象。
- 所有密钥仅由既有安全机制注入进程，不输出、不落盘。不得重复启动付费任务。
- 本次文档清理不授权删除源数据、实验日志、冻结输入、当前论文、源码或既有消融/跨模型证据。
