# 主实验执行进度

**用户最新范围（2026-09-06）：定时任务已取消，当前不继续付费执行。** 用户已明确不再自动安排同一设置的多个 seed，正在重新讨论评价标准、方法对照和 100 户的必要性。原五项串行计划暂停；即使余额恢复，也不自动续跑或启动 seed202。已完成结果和未完成尝试全部保留。下方余额故障与恢复说明是历史记录，不是当前启动授权。

已完成并验收：24 户 seed 7201、8301、9401，各 25 步。当前总进度 3/5。

**停止时的接口故障（2026-09-06）：原 Packy 接口余额不足。** 100 户 seed101 attempt3 完成 24/25 步，在第 25 步收到服务商“用户额度不足”错误，最后返回余额约 −0.11 美元。第 25 步需要 57 份决策，已成功 14 份，还缺 43 份有效响应；该步未提交为完整世界状态。全部 774 条成功响应与原始日志均保留，seed202 尚未启动，当前没有付费实验进程。

已修复中文欠费错误被误当成临时错误而重试的问题，21 项针对性测试通过；774 条缓存离线复用、前 24 步逐字节一致。恢复前后正常提示和成功推理路径不变。余额恢复后从初态在 attempt4 精确复用 attempt3/cache.sqlite，不重付已有成功决策；完成验收后再串行运行原定 seed202。未收到原账户恢复的消息前，不用付费请求探测、不更换模型或供应商。证据：[等待状态](amendments/provider_credit_stop/blocked_state.json)、[重放检查](amendments/provider_credit_stop/replay_checks.json)。

以下为恢复历史，不代表目前仍在运行：

2026-09-06 当前恢复运行：100 户 seed 101 的 attempt3。attempt1 在第 10 步因提供商并发限制中止；并发随后由 12 降到 3。attempt2 完成 11 个时间步，在第 12 步因一名无车居民两次输出步行撤离而未通过既有车辆执行条件检查，停止后续调用。

本次修复显式启用最多两轮模型输出校正，第二轮说明撤离动作的非空车辆条件与当前不支持步行的限制；首轮决策提示、第一次校正提示、JSON Schema 与有效行动规则均保持一致。不会由程序补造车辆、改写动作或兜底通过。42 项针对性检查通过；三个已完成的 24 户运行均离线重放 25 步且日志逐字节一致；100 户前 11 步一致，478 条成功响应全部精确复用。离线重放未使用网络。

attempt3 从初始状态运行，不拼接旧世界状态；原尝试、配置与冻结记录均保留。模型、人口、事件、seed、指标与预算未改。该运行通过完整验收后，控制脚本串行继续 100 户 seed 202，再汇总原定五项。当前仍为 3/5，启动不等于完成。

三次 24 户运行中，首次实际出发的决策成员分别为 15/31、14/31、12/31；全员到模拟安全区的家庭为 11/24、11/24、10/24。三次均有 3/7 多人决策家庭形成过有效约定，实际共同出发为 2/7、1/7、1/7。共同协商在不同 seed 中均有出现，但共同执行覆盖有限，不能据此声称普遍有效协调。

seed 9401 完成 224 次模型决策，无失败或兜底；11 个实际出行队伍的全成员位置、执行时序与路况检查通过，消息图边界及时间顺序、冻结版本核查也通过。其共同出发发生在封路前；封路后另有个人改走备用路线，不能把它写成又一个封路后家庭重新共同出发的例子。

- [实时批次状态](batch_status.json)
- [已完成结果和逐 seed 图表](results/主实验结果.md)
- [seed 101 实时进度](runs/100hh_seed101/attempt3/progress.json)
- [并发修复与离线重放检查](amendments/concurrency_limit_3/offline_replay_check.json)
- [条件校正兼容性检查](amendments/conditional_schema_recovery/compatibility_check.json)
- [四项已有轨迹离线重放](amendments/conditional_schema_recovery/replay_checks.json)
- [指标与执行顺序](指标与执行顺序.md)

全部五项完成后统一整理结果；当前不能写成全部完成。
